# RayLib + ImGui template project

Template project for desktop GUI applications using RayLib and ImGui:

- Implements a model-view-controller pattern.
- Event loop for excheduling events on the main thread.
- Loading vector TTF fonts for the UI and icons (embedded assets).
